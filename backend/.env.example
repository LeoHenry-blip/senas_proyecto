# ============================================================
# .env.example  —  Señas V2
# Copiar como .env y rellenar los valores reales
# ============================================================

# --- MySQL ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=tu_password_aqui
DB_NAME=senas_v2

# --- JWT ---
JWT_SECRET=cambia_esto_por_una_clave_muy_larga_y_aleatoria_en_produccion
JWT_EXPIRE_MIN=1440

# --- IA externa (opcional) ---
IA_API_KEY=

# --- Google OAuth (opcional) ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
